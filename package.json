{
  "name": "sanjeevani",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm.cmd run dev --prefix frontend\" \"npm.cmd run dev --prefix backend\"",
    "build": "npm.cmd run build --prefix frontend && npm.cmd run build --prefix backend",
    "start": "npm.cmd run start --prefix backend",
    "dev:frontend": "npm.cmd run dev --prefix frontend",
    "dev:backend": "npm.cmd run dev --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}